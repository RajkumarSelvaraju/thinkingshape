<div class="container-fluid">
	<div class="row">
		<div class="col-lg-offset-1 col-lg-10">
			<div class="page-header">
				<h1><small>Update Order</small></h1>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="col-lg-offset-2 col-lg-5">
			<%= form_for @order do |f| %>
            	<%= render 'form', :f => f, :order => @order %>
            <% end %>
		</div>
	</div>
</div>